<Container maxW="25%" py={10} justifyContent={"flex-end"}>
        <SignedOut>
          <Box borderWidth="1px" borderColor="gray.200" borderRadius="lg" p={6}>
            <Text fontSize="xl" fontWeight="bold" mb={4}>
              {mode === "signin" ? "Iniciar sesion" : "Crear cuenta"}
            </Text>

            <form onSubmit={handleSubmit}>
              <Stack spacing={4}>
                <FormControl isRequired>
                  <FormLabel>Email</FormLabel>
                  <Input
                    type="email"
                    value={email}
                    onChange={(event) => setEmail(event.target.value)}
                    autoComplete="email"
                  />
                </FormControl>

                <FormControl isRequired>
                  <FormLabel>Contrasena</FormLabel>
                  <Input
                    type="password"
                    value={password}
                    onChange={(event) => setPassword(event.target.value)}
                    autoComplete={mode === "signin" ? "current-password" : "new-password"}
                  />
                </FormControl>

                {mode === "signup" && isVerifying ? (
                  <FormControl isRequired>
                    <FormLabel>Codigo de verificacion</FormLabel>
                    <Input
                      value={code}
                      onChange={(event) => setCode(event.target.value)}
                      inputMode="numeric"
                    />
                  </FormControl>
                ) : null}

                {error ? <Text color="red.500">{error}</Text> : null}

                <Button type="submit" colorScheme="blue" isLoading={isLoading}>
                  {mode === "signin"
                    ? "Entrar"
                    : isVerifying
                    ? "Verificar codigo"
                    : "Registrarme"}
                </Button>

                <Button
                  variant="link"
                  onClick={() =>
                    resetStateForMode(mode === "signin" ? "signup" : "signin")
                  }
                >
                  {mode === "signin"
                    ? "No tienes cuenta? Registrate"
                    : "Ya tienes cuenta? Inicia sesion"}
                </Button>
              </Stack>
            </form>
          </Box>
        </SignedOut>

        <SignedIn>
          <Text>Sesion iniciada.</Text>
        </SignedIn>
      </Container>